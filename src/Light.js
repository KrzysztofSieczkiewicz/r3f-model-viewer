export function Lights() {
    const types = {
        pointLight: "pointLight",
        spotLight: "spotLight",
        rectAreaLight: "rectAreaLight",
    }

    const [type, setType] = useState();

    return;
}